version: '3.8'

services:
  chatbot:
    build: .
    container_name: flask-gemini-chatbot
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped